server:
  port: 9103
  forward-headers-strategy: framework  # Required per constitution §3.2

spring:
  application:
    name: oauth2-backend

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope: openid,email,profile
            redirect-uri: https://backend.brunorozendo.dev/login/oauth2/code/google
            authorization-grant-type: authorization_code
        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
            jwk-set-uri: https://www.googleapis.com/oauth2/v3/certs

app:
  frontend:
    origin: https://frontend.brunorozendo.dev
  jwt:
    secret: ${JWT_SECRET:default-dev-secret-must-be-at-least-32-bytes-long!!}
    expiration-seconds: ${JWT_EXPIRATION_SECONDS:60}
    cookie-name: AUTH_TOKEN

logging:
  level:
    com.brunorozendo.oauth2: DEBUG
    org.springframework.security: DEBUG
